import { getAdminOverview } from '@/lib/admin'

export default async function AdminPage() {
  const overview = await getAdminOverview()
  const cards = [['Total users', overview.total], ['Active accounts', overview.active], ['Administrators', overview.admins], ['Pilot tier', overview.pilots]]
  return <section><div className="mb-8"><p className="font-mono text-[10px] uppercase tracking-[0.18em] text-text-dim">Operations snapshot</p><h2 className="mt-2 text-3xl font-semibold tracking-tight">System overview</h2><p className="mt-2 max-w-2xl text-sm leading-6 text-text-muted">A grounded view of access, account state, and integration readiness.</p></div><div className="grid gap-3 sm:grid-cols-2 xl:grid-cols-4">{cards.map(([label, value]) => <div key={label} className="rounded-lg border border-border-default bg-bg-surface p-5"><p className="font-mono text-[10px] uppercase tracking-[0.12em] text-text-dim">{label}</p><p className="mt-4 text-3xl font-semibold">{value}</p></div>)}</div><div className="mt-8 grid gap-4 md:grid-cols-2"><div className="rounded-lg border border-border-default bg-bg-surface p-5"><p className="font-mono text-[10px] uppercase tracking-[0.12em] text-text-dim">Access model</p><p className="mt-3 text-sm leading-6 text-text-secondary">Admin access is persisted on the Better Auth user record. No email-based or client-only escalation is used.</p></div><div className="rounded-lg border border-border-default bg-bg-surface p-5"><p className="font-mono text-[10px] uppercase tracking-[0.12em] text-text-dim">Batana integration</p><p className="mt-3 text-sm leading-6 text-text-secondary">Not available in Phase 1. Requests and external integration controls remain read-only until a real contract exists.</p></div></div></section>
}
