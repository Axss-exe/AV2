import { getAdminOverview } from '@/lib/admin'

export default async function AdminSystemPage() { const overview = await getAdminOverview(); return <section><p className="font-mono text-[10px] uppercase tracking-[0.18em] text-text-dim">Runtime health</p><h2 className="mt-2 text-3xl font-semibold tracking-tight">System status</h2><div className="mt-8 grid gap-3 sm:grid-cols-2"><Status label="Database" value="Operational" /><Status label="Analysis pipeline" value="External" /><Status label="Batana" value="Not available" /><Status label="Audit log" value={overview.integrations === 'not_available' ? 'Not available' : 'Ready'} /></div></section> }
function Status({ label, value }: { label: string; value: string }) { return <div className="flex items-center justify-between rounded-lg border border-border-default bg-bg-surface p-5"><div><p className="font-mono text-[10px] uppercase tracking-[0.12em] text-text-dim">{label}</p><p className="mt-2 text-sm">{value}</p></div><span className="h-2 w-2 rounded-full bg-text-primary" aria-hidden="true" /></div> }
